name: on-workflow-dispacth

on:
  workflow_dispatch:
    
jobs:
  test-job:
    runs-on: self-hosted
    steps:
      - name: checkout code
        uses: actions/checkout@v5
      - name: execute bash script
        run: |
          echo "hello test-job"