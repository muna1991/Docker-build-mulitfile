name: on-workflow-dispatch-example

on:
  push:
    branches:
      - feature/new-demo-test
  workflow_dispatch:

jobs:
  test-job:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Execute bash script
        run: echo "test-job"
