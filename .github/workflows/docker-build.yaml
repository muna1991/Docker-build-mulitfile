name: Build and Deploy

on:
  push:
    branches:
      - main
    paths:
      - docker-config-*.yaml
jobs:
  build-and-push:
    uses: muna1991/docker-lib-multifile-build/.github/workflows/dokcer-library.yaml@main 
    with:
      branch: "main"
      paths: "docker-config-*.yaml"
