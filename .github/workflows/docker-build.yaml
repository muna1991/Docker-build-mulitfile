name: Build Docker Image with Docker-in-Docker

on:
  push:

jobs:
  docker-build:
    runs-on: self-hosted
    steps:
      - name: check docker steps
        run: |
          docker image ls

  

  