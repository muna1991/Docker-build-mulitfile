name: Build Docker Image with Docker-in-Docker

on:
  push:

jobs:
  docker-build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: build Docker image
        run: |
          docker image build -t new:v1 .
          

  

  