name: Build, Scan, and Push Docker Images (Reusable)
on:
  push:

jobs:
  docker-build:
    runs-on: self-hosted
    steps:
      - name: build docker image
        run: |
          docker image ls
  

  